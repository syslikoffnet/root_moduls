# v5.0.1 — Ultimate by handsgod (сборка одного целого)

- Рантайм гео-движка вынесен из каталога модуля в /data/adb/geo-unblock
  (переживает обновления; конфиг с паролями нод больше не лежит в module-dir)
- Токены подписок маскируются в логах (URL обрезается до «?»)
- service-watch перезапускает гео-watchdog, если тот убит (OOM/kill) —
  fail-open больше не зависит от выживания одного процесса
- Обновление с v5.0.0 корректно останавливает гео-движок прежней версии
- WebUI: секция «Гео-прокси» переработана — статус-пилюли, кнопка
  «Страна выхода», аккуратные сегменты, счётчик выбранных приложений
- module.prop: чистое описание, v5.0.1

# v5.0.0 — Ultimate by handsgod (слияние модулей)

- Встроенный гео-прокси (sing-box TPROXY, без TUN): авто-подписки
  (igareck/vpn-configs-for-russia), классы wifi/mobile/all, urltest по лучшему
  пингу, отбор приложений, импорт из Happ (URL/base64/строки)
- Единый WebUI (раздел «Гео-прокси»), geo-команды в api.sh
- Инсталлер: скачивание и SHA-256-проверка sing-box по ABI; авто-удаление
  прежнего отдельного geo-unblock с сохранением его данных
- Безопасность: прокси-порт 127.0.0.1:7890 закрыт от чужих приложений,
  строгая валидация ввода, идемпотентный старт гео-движка

# Список изменений zapret2-android v4.0.5 (4005)

Исправления по результатам сплошного разбора кода и проверки на устройстве
(realme PJA110, Android 16, KernelSU, две SIM, WARP включён).

## Списки доменов доезжают до nfqws2 во всех движках

`build_hostlist_args` вычислялся и выбрасывался в `SMART_NATIVE` и `CUSTOM`:
`--hostlist`, `--hostlist-auto` и обучение в `state/auto.list` не передавались
вообще. `SMART_NATIVE` — предпочитаемый движок на любом ядре с
`nf_conntrack_acct`, `xt_connbytes` и `CONNMARK`, то есть на исправном ядре
ключевая механика v4.0.0 была выключена, а лог рапортовал успех.

## Исключения действуют в каждом профиле

`DESYNC_ARGS_*` содержат собственные `--new`, то есть описывают несколько
профилей, а фильтры внутри профиля не наследуются. `--hostlist-exclude` и
`--ipset-exclude`, указанные один раз, защищали только первый из них: домен из
`exclude_domains.list` (банки, госуслуги — 311 записей в поставке) спокойно
попадал под десинхронизацию в следующем профиле.

Заодно `HOSTLIST_MODE` наконец означает то, что написано: режимы `LIST` и
`AUTO` были неотличимы от `ALL`, потому что общий профиль ловил всё подряд.
Кому нужно прежнее поведение — `HOSTLIST_MODE="ALL"`.

Профиль подсетей теперь идёт первым, как в nfqws2-keenetic: адрес назначения
известен раньше имени хоста из ClientHello.

## Проверка кэшированной стратегии заработала

`save_cache` читала список контрольных хостов из `$6`, а все семь вызовов
передавали его пятым аргументом. `VERIFY_HOSTS` всегда оставался пустым, и
`verify_cached_strategy` уходила в ветку «проверять нечем, профиль принят как
есть» — то есть проверка не выполнялась ни разу. Сломанный профиль на
сменившейся сети не браковался до истечения TTL (сутки на Wi-Fi).

## Модуль переживает неудачный перезапуск

Сторожевые процессы гасились в начале реконсиляции, а поднимались в самом
конце. Все аварийные выходы между этими точками (нет NFQUEUE, nfqws2 не
стартовал) оставляли модуль без надзора: ни самовосстановления, ни реакции на
смену сети до перезагрузки телефона. Подъём вынесен в ловушку `EXIT`.

Чтобы это не превратилось в перезапуск каждые две минуты на несовместимом
ядре, добавлена пауза `HEALTH_ERROR_BACKOFF` (10 минут) после старта,
завершившегося ошибкой.

## Сетевой watcher больше не жжёт батарею при устойчивом отказе

При неудаче применения правил повтор шёл каждые две секунды бесконечно, и
каждый повтор выполнял два `dumpsys`. Добавлено удвоение задержки до
`VPN_RETRY_MAX` (5 минут) и отказ от дорогой проверки ролей на повторе.

## WARP

- `WARP_STARTUP_TRIES` не действовал: две проверки против размера матрицы
  подряд всегда давали 40. Стартовый перебор всегда шёл на полную.
- Сторож вызывал подъём туннеля синхронно, и на время перебора (до 40
  кандидатов по несколько секунд) без надзора оставались nfqws2 и очередь
  NFQUEUE. Подъём уехал в фон.
- `acquire_warp_lock` мог крутиться без предела и без паузы.

## WARP засыпает, когда нет несущей сети

Замерено на устройстве в авиарежиме: сторож честно шагал по матрице профилей —
step 6, 7, 8, 9, 10, 11, 12 за три минуты, — хотя ни один кандидат не мог
получить рукопожатие, потому что пакету некуда идти. Дойдя до сорокового
профиля, он поставил бы `result=failed`, выждал `WARP_ADAPT_RETRY_SEC` и начал
заново; плюс раз в `WARP_STALL_RESTART_SEC` поднимал бы весь туннель с нуля.
В самолёте, метро или подвале это продолжалось бы до возвращения сети.

Добавлена проверка `underlay_available`: есть ли вообще маршрут до endpoint'а
(и не ведёт ли он в сам туннель). Без несущей сети перебор и полный перезапуск
приостанавливаются, в журнал один раз пишется причина. Возвращение сети снимает
паузу. Отметка «нездоров с такого-то момента» продолжает идти, поэтому после
длинного обрыва туннель всё же поднимается с нуля — состояние интерфейса за это
время всё равно устарело.

Заодно маршруты по адресу назначения снимаются один раз, а не на каждом тике:
прежде эта ветка при каждой проверке заново обходила пятнадцать подсетей и
писала строку в журнал, даже когда снимать уже было нечего.

## Правки списков вне WebUI подхватываются

`ipset.list` и `ipset_exclude.list` — основной способ отбора по подсетям с
v4.0.0 — отсутствовали и в ветках `on_change.sh`, и в формуле подписи
конфигурации. Правка через файловый менеджер или `adb push` не вызывала
ничего. Оба набора приведены к одному составу; удалённые в v4.0.0 имена убраны.

## Прочее

- WebUI не мог прочитать ни одного статуса через HTTP: страница слала
  read-only запросы через GET без обязательного заголовка `X-Zapret2-WebUI`.
- `json-strategies` печатал два JSON-объекта подряд (13 спецификаторов на 15
  аргументов) и путал поля.
- Гонка во всех четырёх блокировках: пустой `pid` считался брошенной
  блокировкой, хотя владелец мог только что сделать `mkdir`.
- `auto-select` зависел от `xt_owner`, проверку которого убрали в v4.0.0
  вместе с правилами по приложениям; теперь отсутствие модуля видно как
  `AUTO_STATUS=NO_OWNER_MATCH`, а не как невнятная ошибка baseline.
- Проверка дрейфа конфигурации не срабатывает, пока идёт реконсиляция: reload
  занимает 45-90 секунд, и health-watcher успевал запустить второй поверх
  первого.
- `ensure_httpd` звал `pid_cmdline` для каждого процесса в системе (700-900
  форков на один reload).
- 38 циклов удаления правил без предела в `uninstall.sh` и `warp-tunnel.sh`
  переведены на ограниченный помощник.
- `run/webui.token` генерировался и никогда не проверялся — убран.
- Две испорченные строки с литеральным `
`: в установщике она превращала
  аргумент `sed` в имя файла, в `auto-select.sh` объявляла лишнюю переменную.
- `WARP_SIP_FORCE` не переносился при обновлении и молча сбрасывался.
- Удалены осиротевшие `sni_routes.conf` и `proxies.conf` от вырезанного
  AI Router.

# Список изменений zapret2-android v4.0.0 (4000)

Отбор трафика переведён на принцип nfqws2-keenetic: только домены и подсети.
Разделение по приложениям убрано полностью.

## Почему

Каталог приложений неизбежно отстаёт от того, что реально установлено. На
тестовом устройстве из 74 записей каталога совпали три, причём одна из них была
уведена в туннель — то есть обход DPI доставался двум почти неиспользуемым
приложениям. Правило по UID вдобавок не помогает, если стоит другой клиент того
же сервиса. Домен же заблокирован одинаково для всех, кто к нему ходит.

Замер расхода на системном режиме: 125 исходящих пакетов на мегабайт трафика,
то есть 2–6 секунд процессорного времени на гигабайт. Батарея тут не аргумент.

## Что изменилось

- **Одно правило netfilter на всю систему** вместо режимов INCLUDE / EXCLUDE /
  GLOBAL. Решение «вмешиваться или нет» целиком принимает nfqws2.
- **Домены**: `user.list`, `exclude_domains.list` и обучаемый `state/auto.list`.
- **Подсети**: `ipset.list` и `ipset_exclude.list` — аналог одноимённых списков
  из nfqws2-keenetic. Вынесены в отдельный профиль nfqws2, потому что внутри
  одного профиля `--hostlist` и `--ipset` складываются по И, а нужно ИЛИ.
- **Туннель WARP** тоже маршрутизируется по доменам и подсетям. Telegram уходит
  туда своими диапазонами, что работает для любого клиента, а не только для того,
  чей пакет был в списке.
- **QUIC**: блокировать или нет. Имя хоста в UDP/443 недоступно, поэтому
  выборочность там была фикцией.
- **WebUI**: вкладка приложений заменена редактором списков; добавлена кнопка
  перезапуска туннеля.

## Что удалено

Разрешение UID целиком: разбор `/data/system/packages.list`, опрос
PackageManager, обход профилей пользователя, кэш и его доверификация. Вместе с
ним ушли `app-sync.sh`, проверка `xt_owner`, ключи `MODE`, `SCOPE_PRESET`,
`AUTO_APPS_ENABLED`, `PACKAGE_WAIT_SECONDS` и восемь списков приложений.
Суммарно около 900 строк.

## Что нужно сделать после обновления

Приложения из прежнего `exclude.list` (банки, госуслуги) больше не выводятся
из-под обхода по UID. Их домены нужно внести в `lists/exclude_domains.list`, а
адреса — в `lists/ipset_exclude.list`. Установщик сообщит, сколько записей было.

## Исправления WARP

- Watchdog больше не молчит, когда не может взять блокировку: стартовый перебор
  профилей держит её минутами, и со стороны это выглядело как зависший на одном
  шаге туннель.
- Если туннель не оживает дольше `WARP_STALL_RESTART_SEC` (10 минут), модуль
  перезапускает его целиком и сбрасывает перебор на начало.
- Добавлена команда `warp-restart` и кнопка в WebUI.

# Список изменений zapret2-android v3.7.2 (3702)

- **Маршруты в туннель ставятся только при живом туннеле.** Раньше правила по
  адресу назначения существовали независимо от состояния WARP. Если интерфейс
  поднят, а связи нет, трафик уходил в него и пропадал — замерено на устройстве:
  `ip route get` отдавал `dev awg99` при отсутствующем рукопожатии, то есть
  Telegram полностью терял связь, хотя без туннеля получил бы обычный обход DPI.
  Теперь при неработающем туннеле такие маршруты снимаются, и подсети с доменами
  возвращаются на обычный путь. Приложения из `warp_apps.list` остаются в
  туннеле: их увели туда сознательно, и тихо выпускать их наружу нельзя.

- **Живость туннеля определяется по прохождению данных, а не только по
  рукопожатию.** Бывает состояние, когда пир исправно отвечает на рукопожатия, а
  полезный трафик молча теряет. Проверка отправляет собственный пакет с
  привязкой к интерфейсу туннеля, поэтому простой не путается с обрывом.

- **Подсети туннеля больше не выводятся из-под обхода DPI по адресу назначения.**
  Всё, что уходит в туннель, уже покрыто правилом на интерфейс `awg99` (проверено
  счётчиками: тысячи совпадений). А отдельный RETURN по адресу назначения был
  вреден: когда туннель падал и трафик возвращался на обычный маршрут, эти
  подсети оставались вообще без обхода.

- **Исправлен разбор счётчиков туннеля.** `awg show <dev> transfer` в текущей
  сборке `awg` игнорирует подкоманду и отдаёт полный дамп, поэтому разбор по
  номерам полей возвращал нули.

- **`warp_domains.list` не вызывал реконсиляцию** — имя не попадало ни под один
  шаблон в обработчике inotify, изменения подхватывались только страховочной
  проверкой контрольной суммы.

- **Формула контрольной суммы конфигурации сведена в одно место**
  (`strategy-lib.sh`). Раньше она была скопирована в два файла: любое расхождение
  в составе или порядке файлов превращалось в бесконечный цикл перезагрузок.
  Лёгкая реконсиляция теперь сама освежает подпись, поэтому правка списка
  приложений больше не вызывает вдогонку ещё и полный перезапуск.

# Список изменений zapret2-android v3.6.0 (3600)

- **Домены, которые не лечит обход, уводятся в туннель WARP.**
  - Если ни один профиль обхода не поднял домен, модуль сам решает, поможет ли
    туннель, и при положительном ответе добавляет домен в маршрутизацию через
    WARP (`state/warp_auto_domains.list`). Ручной список — `lists/warp_domains.list`.
  - Гео-блокировки пропускаются: если сервер отвечает 403/451 по честному
    сертификату, значит путь до него есть и блокирует сам сайт. Туннель выходит
    в той же стране и делу не поможет, поэтому такие домены не трогаются.
    Признак блокировки на пути — отсутствие HTTP-ответа вовсе либо подменённый
    сертификат; вот они и уходят в туннель.
  - Ключи: `WARP_DOMAIN_ROUTING`, `WARP_DOMAIN_FALLBACK`.
  - Ограничение: маршрут строится по IP, а у сайтов за CDN адреса меняются и
    делятся с другими сайтами. Список перерезолвивается при каждой синхронизации
    и при смене сети, но абсолютной точности здесь быть не может.

- **Список подсетей туннеля перестал дублироваться.** Диапазоны Telegram были
  захардкожены в трёх местах (`service.sh`, `app-sync.sh`, `warp-tunnel.sh`),
  и наборы успели разойтись. Теперь источник один — `lists/warp_bypass_nets.list`.
  Старые правила от прежних версий снимаются при очистке.

- **Исправлена классификация двух списков в обработчике inotify.** `user.list` и
  `warp_bypass_nets.list` не попадали ни под один шаблон и не вызывали
  реконсиляцию: изменения подхватывались только страховочной проверкой
  контрольной суммы, с задержкой до минуты. Шаблон `bypass_nets*.list` не
  покрывал имя `warp_bypass_nets.list`, потому что сопоставление идёт с именем
  целиком.

# Список изменений zapret2-android v3.4.0 (3400)

Ревизия по итогам полного аудита кода. Правки затрагивают поведение по
умолчанию — прочитайте раздел «Что изменилось в поведении».

- **Автоподбор стратегий заработал как задумано.**
  - Эталон «без обхода» теперь действительно снимается без обхода: функция
    установки чистого правила была объявлена, но ни разу не вызывалась. В
    режимах GLOBAL/EXCLUDE эталон измерялся через уже работающий nfqws2, из-за
    чего модуль решал, что сеть не фильтруется, и уходил в DIRECT.
  - Восстановлено отбраковывание кандидатов с ошибкой Lua: путь к логу nfqws2 в
    auto-select.sh не был задан, проверка всегда возвращала «ошибок нет», и
    заведомо сломанная стратегия проходила отбор.
  - Проверка хоста считается успешной при любом ответе 2xx/3xx, а не строго 200.
    Прежний `probe_hosts.list` требовал ровно 200 от доменов, которые отдают
    редирект или 404 (`googlevideo.com`, `instagram.com`), — такие хосты
    считались заблокированными всегда, эталон никогда не был чистым, и ни одна
    стратегия не могла набрать полный балл.
  - Тестовые правила используют реально поддерживаемый ядром `--queue-bypass`
    (определяется при старте), а не ставят флаг вслепую.
  - Отсутствие `curl` в прошивке больше не отключает автоподбор молча: статус
    `NO_CURL` виден в WebUI.
  - Если сеть чистая, а DIRECT запрещён настройкой, перебор кандидатов
    пропускается — чинить нечего.

- **Изменения списков снова подхватываются на лету.**
  - Обработчик inotifyd читал маску события вместо имени файла, поэтому любая
    правка любого списка приводила к полному перезапуску службы вместо горячей
    пересборки правил приложений.
  - Наблюдение переведено с отдельных файлов на каталоги: все писатели меняют
    файлы атомарно (`mv -f`), и watch на файле навсегда «залипал» на удалённом
    иноде после первой же правки.
  - Добавлена страховка: health-watcher раз в минуту сверяет контрольную сумму
    конфига и списков и запускает реконсиляцию, если правку не увидел inotify.

- **Смена сети отслеживается независимо от раздачи.** Сетевой watcher запускался
  только при включённой раздаче, хотя именно он будит автоподбор при смене
  Wi-Fi/соты и синхронизирует туннель. Выключение раздачи отключало реакцию на
  смену сети целиком.

- **Быстрая смена профиля больше не затирает выбор пользователя.** Она молча
  подставляла COMPAT-профиль независимо от режима, перезаписывая CUSTOM-аргументы
  и подменяя стратегию в SMART_NATIVE. Теперь применяется только там, где
  per-network профили и предусмотрены.

- **Telegram получает обход, когда туннель выключен.** Список подсетей был
  захардкожен и выводил их из-под AntiDPI всегда, в том числе при выключенном
  WARP, — то есть Telegram не получал ни обхода DPI, ни блокировки QUIC. Список
  вынесен в `lists/warp_bypass_nets.list`, применяется только вместе с туннелем и
  дополнен опубликованными диапазонами.

- **WebUI.**
  - Применение настроек больше не держит HTTP-соединение весь перезапуск службы:
    ответ возвращается сразу, прогресс отображается из состояния запуска.
  - HTTP-сервер стал опцией `ENABLE_HTTP_API` (по умолчанию выключен): у
    KernelSU / APatch / MMRL работает нативный мост, и слушающий сокет не нужен.
  - Проверка происхождения запроса распространена на чтение: `json-apps`,
    `log` и `nfqws-log` отдавали список приложений, SSID и оператора любому GET.
  - Перезапуск службы больше не убивает веб-сервер, который этот перезапуск и
    запросил (после чего порт мог смениться с 8080 на 8088).
  - Исправлена подсказка, предлагавшая открывать WebUI по локальному IP телефона:
    сервер намеренно слушает только loopback.

- **Установщик.**
  - Перенос настроек ограничен списком пользовательских ключей. Раньше
    переносились все строки `KEY="..."`, включая встроенные профили обхода, —
    при обновлении они всегда перетирались старыми значениями.
  - Кэш автоподбора больше не стирается сразу после восстановления из бэкапа.
  - Пользовательские стратегии сохраняются в рабочем формате: диапазон
    `strategy_100` и выше зарезервирован за пользователем (см. `strategies/README.txt`).
  - Один вопрос вместо четырёх: при установке через WebUI менеджера кнопки
    громкости не работают, и каждый вопрос просто выжидал таймаут.

- **Надёжность.**
  - Боевой и тестовый nfqws2 различаются по номеру очереди: перезапуск во время
    подбора больше не подхватывает PID тестового процесса и не убивает его.
  - Прерванная горячая пересборка правил не оставляет трафик без обхода, а
    незавершённые цепочки убираются при очистке и удалении модуля.
  - Запуск nfqws2 сведён в одну функцию (было восемь разошедшихся копий).
  - Убран дублирующий watchdog туннеля и повторное вычисление UID.

## Что изменилось в поведении

- `ENABLE_WARP` снова `0`. Включение регистрирует аккаунт Cloudflare WARP и
  поднимает туннель — это делается явно, из WebUI.
- `LOG_EXPORT_ON_BOOT="0"`: логи больше не копируются автоматически в
  `/sdcard/eCubz` при каждой загрузке. Кнопка «Экспорт логов» работает как прежде.
- `ENABLE_HTTP_API="0"`: доступ к WebUI из браузера включается вручную.

# v3.3.8 (3245) — Telegram Subnet Routing & Canonical Junk Recovery

- **Восстановление маршрутизации подсетей Telegram (`TG_SUBNETS`):**
  - Восстановлена прямая маршрутизация по диапазонам IP дата-центров Telegram (`91.108.0.0/16`, `149.154.160.0/20`, `185.76.151.0/24`, `95.161.64.0/20`, `2001:b28:f23d::/48`, `2001:67c:4e8::/48`) в туннель WARP (`awg99`) с высоким приоритетом (`pref 40`).
  - Устранена проблема игнорирования маршрутизации ядром Linux при принудительном сокетном байнде Android (`SO_BINDTODEVICE = rmnet_data*`).
- **Канонические параметры AmneziaWG для Cloudflare WARP:**
  - Установлены эталонные рабочие параметры: `Jc=5`, `Jmin=40`, `Jmax=70`, `S1=0`, `S2=0`, `H1=1..H4=4`, `Endpoint=162.159.192.1:2408` без паразитных SIP-заголовков.
- **Проверка L7 HTTPS трафика в адаптивном сканере:**
  - В `probe_handshake` добавлена валидация реальной передачи данных по HTTPS (`https://1.1.1.1/`), исключающая выбор эндпоинтов с «пустым» рукопожатием.

# v3.3.7 (3244) — WebUI JSON Fix & Strategy Polish

- **Исправление парсинга JSON в WebUI:**
  - В `bin/zapret2-control` заменён подсчёт строк на базе `grep -cvE` на безопасный `awk`, устраняющий появление паразитных переносов строк вида `0\n0` при пустых списках приложений. Полностью устранена ошибка `Expected ',' or '}' after property value in JSON at position 1662`.
- **Исправление стратегий и стабильности браузеров:**
  - В `strategies/strategy_2` удалён глобальный подлог `sni=www.google.com`, приводивший к сбросу TLS-рукопожатий на серверах Microsoft (Edge Canary, Bing, Live sync).
  - Снижено число повторов фейковых пакетов (`repeats=2`), предотвращающее сброс сессий сетевыми DPI-фильтрами.
- **Корректировка цепочек AntiDPI для Telegram:**
  - Из `service.sh` в `ZAPRET2_MANGLE` удалены жесткие правила безусловного пропуска подсетей Telegram (`149.154.160.0/20`, `91.108.0.0/16`), что восстановило обработку пакетов `nfqws2`.
- **Очистка кэша маршрутизации:**
  - Добавлена очистка кэша маршрутов (`ip route flush cache`) при переключении правил и интерфейсов.

# v3.3.6 (3242) — Telegram Media Download & Clean DNS Fix

- **Исправление загрузки медиафайлов и фото в Telegram / Exteragram:**
  - В `warp-tunnel.sh` исправлен сбор DNS-серверов (`collect_warp_dns`): теперь в первую очередь используются надёжные DNS самого туннеля (`1.1.1.1`, `1.0.0.1`, `8.8.8.8`, `8.8.4.4`), что устранило поломку резолва Telegram CDN хостов (`cdn1.telegram.org` ... `cdn5.telegram.org`).
  - Внедрён мгновенный ICMPv6 Reject для WARP-приложений (`ip6tables -t filter -A ZAPRET2_WARP_FILTER -m owner --uid-owner <uid> -j REJECT --reject-with icmp6-port-unreachable`), устраняющий зависание механизма Happy Eyeballs на недоступных IPv6 эндпоинтах Telegram.
  - Добавлена явная фиксация `TCPMSS = 1240` на интерфейсе `awg99` для предотвращения фрагментации больших пакетов медиа-потоков в мобильных сетях.
- **Очистка конфигурации DNS (`lists/dns.list` и `zapret2.conf`):**
  - Из установочного списка `dns.list` удалены сторонние прокси-серверы; оставлены только эталонные резолверы Cloudflare (`1.1.1.1`, `1.0.0.1`) и Google (`8.8.8.8`, `8.8.4.4`).
  - Параметры `DNS_FORWARD_SERVERS` и `WARP_DNS` в `zapret2.conf` синхронизированы на чистый список `1.1.1.1 1.0.0.1 8.8.8.8 8.8.4.4`.

# v3.3.5 (3241) — Live ADB Fix & WARP Routing Recovery

- **Устранение критической ошибки подбора WARP (AmneziaWG v3):**
  - Исправлена несовместимость со встроенным бинарником `awg`: заменены неподдерживаемые команды (`syncconf`, `set`, `latest-handshakes`, `transfer`) на атомарный `awg setconf` и прямой парсинг параметров `awg show`.
  - В пул авто-подбора эндпоинтов интегрированы европейские IP-адреса Cloudflare (`188.114.97.1:2408`, `188.114.96.1:500`, `162.159.193.1:4500` и пользовательский), что обходит блокировку подсети `162.159.192.0/24` провайдерами РФ.
  - Оптимизирован порядок матрицы `candidate_meta()`: в первую очередь опрашиваются проверенные серверы на лёгком профиле мусора (`Jc=5..10`), что сократило время подключения до 1–4 секунд.
  - Внедрена активная сетевая посылка зонда (`ping -c 1 -W 1 -I awg99 1.1.1.1`) для мгновенного триггера Handshake Initiation в `amneziawg-go`.
  - Устранена ошибка ложноположительного срабатывания таймстампа рукопожатия и добавлена валидация роста `rx_bytes`.
  - В `generate_warp_config` добавлен резервный `--resolve` при отправке PATCH-запроса на активацию `warp_enabled`.
  - Значение `Jmax` ограничено по MTU подсети (`Underlay_MTU - 100 <= 1280`), исключая фрагментацию UDP в LTE/5G.
- **Исправление системы автоподбора стратегий AntiDPI (`auto-select.sh`):**
  - Исправлен вызов `score_against_baseline "$CANDIDATE_FILE" "$BASELINE_FILE"` (восстановлен утерянный второй аргумент и вывод результатов скоринга). Автоподбор теперь реально выбирает лучшую стратегию вместо постоянного отката к дефолту.
  - Исправлен вызов `log_size "$NFQWS_LOG"`.
- **Маршрутизация и безопасность:**
  - В `net-role.sh` интерфейс `awg99` исключён из кандидатов внешнего VPN-шлюза для Hotspot-раздачи.
  - В `uninstall.sh` добавлена полная очистка правил `ZAPRET2_WARP_DNS` (таблица `nat`) и `POSTROUTING` mangle.
  - В `diagnostics.sh` добавлен подробный раздел диагностики состояния WARP, таблицы маршрутизации и хендшейков.

# v3.3.4 (3240) — WARP adaptive hang fix

- Исправлено зависание AUTO на `1/40`: локальные UAPI-команды `awg show/set/syncconf/setconf` теперь имеют жёсткий таймаут и не могут навечно удерживать `warp.lock`.
- Ошибка применения отдельного J/port/SIP-кандидата больше не обрывает всю матрицу с вечным `result=pending`: кандидат помечается как неуспешный и перебор идёт дальше до 40/40 либо handshake.
- Прогресс шага записывается до обращения к UAPI, поэтому WebUI показывает реальное продвижение даже при отвергнутом профиле.
- Endpoint по-прежнему допускает IP или hostname в ручном поле, но AUTO по умолчанию использует consumer-WARP ingress IP, чтобы запуск туннеля не зависел от DNS до появления самого туннеля.

# v3.3.3 (3230) — WARP adaptive/UI recovery fix

- Исправлен вечный статус «Поиск рабочего профиля»: первичный AUTO-поиск теперь завершает всю ограниченную матрицу из 40 кандидатов и явно переходит в `failed`, если handshake не найден. После полного провала действует backoff, а не бесконечная ротация.
- `service-watch.sh` и `vpn-watch.sh` перечитывают `zapret2.conf` во время работы: включение WARP из WebUI после загрузки Android больше не оставляет watcher со старым `ENABLE_WARP=0`.
- Исправлен WebUI live-polling: несохранённые WARP-поля больше не перезаписываются каждые 2.5 секунды. Отдельно отображаются сохранённый baseline и реально тестируемый AUTO-профиль.
- Добавлены режимы `AUTO` и `Ручной`. В ручном режиме Jc/Jmin/Jmax, endpoint/port остаются строго пользовательскими; I1/I2 можно принудительно включить отдельным переключателем.
- AUTO использует конечную матрицу: сначала все 20 BASIC-комбинаций (5 J-профилей × 4 WARP-порта), и только если ни одна не дала handshake — 20 тех же комбинаций с SIP I1/I2. В набор включён совместимый со старыми рабочими конфигами профиль `Jc=10, Jmin≈800, Jmax≈1400` с ограничением Jmax по MTU.
- S1/S2/H1-H4 не рандомизируются для Cloudflare WARP: сохраняются WireGuard-совместимые значения; адаптируются только клиентские junk/signature параметры и endpoint port.
- Сохранение WARP через HTTP/WebRoot больше не ждёт долгий adaptive scan: restart запускается асинхронно, UI получает ответ сразу.
- Усилена генерация WARP peer-конфига: PublicKey пира извлекается из блока peers, с известным consumer-WARP peer key как безопасным fallback; PrivateKey по-прежнему генерируется индивидуально на устройстве и в ZIP не поставляется.

# Список изменений zapret2-android v3.3.1 (3210)

- **Исправления по повторному полному аудиту v3.3.0:**
  - WebUI HTTP fallback синхронизирован с реальными командами контроллера и аргументами; write-команды только POST, без `eval`, wildcard CORS и без логина/пароля. Добавлена прозрачная CSRF-защита служебным заголовком WebUI.
  - Обновление сохраняет списки приложений, исключений, WARP и DNS, а установщик проверяет SHA-256 выбранных ABI-бинарников по manifest.
  - WARP: атомарная синхронизация UID, динамические Android users, PID-owned/re-entrant lock, точная очистка policy rules, проверка маршрутизации, fail-closed IPv6, безопасная регистрация/rekey и DNS forcing для обычного UDP/TCP 53.
  - Удалён готовый `state/warp.conf` из релиза: приватный WARP-ключ теперь всегда создаётся индивидуально на устройстве.
  - Исправлены DIRECT+tethering NFQUEUE, IPv6 VPN leak guard, гонка `on_change`, транзакционное обновление app chains и диагностика/экспорт логов.
  - Удалены устаревшие AI Router cleanup-пути, broad process kill, `eval` в watcher/auto-select и неиспользуемые WARP-параметры.

# Список изменений zapret2-android v3.3.0 (3200)

- **Полный аудит безопасности и надежности (F01–F38):**
  - **Безопасность WebUI:** Полный отказ от `eval`, внедрён строгий whitelist команд и параметров, ограничение размера POST-тела, привязка BusyBox httpd строго к `127.0.0.1`.
  - **Управление сетевыми правилами:** Выделенные изолированные цепочки iptables/ip6tables `ZAPRET2_WARP_MANGLE` с атомарным обновлением и корректной очисткой всех правил `ip rule` (включая IPv6 Telegram).
  - **Надежность процессов:** Безопасное завершение процессов без `killall`, строгая проверка владельца PID в `/proc`.
  - **Раздача интернета (Tethering):** Режим `DIRECT` теперь пропускает только AntiDPI nfqws2, сохраняя полную работоспособность раздачи (Hotspot / VPN sharing). Усилена защита от IPv6-утечек (Fail-Closed).
  - **Автоподбор:** Исправлено определение доступности смешанных IPv4/IPv6 хостов и проверка актуальности DIRECT кэша.
  - **Удаление подсистемы AI Router:** Модуль полностью сфокусирован на AntiDPI и точечном туннеле. Обеспечена автоматическая очистка устаревших файлов и процессов AI Router при обновлении.

# Список изменений zapret2-android v3.2.30 (3150)

- **Исправление HTTP Basic Auth в WebUI:** Устранено ложное срабатывание системного диалога авторизации («Войти / Имя пользователя и пароль») при применении настроек и пресетов в Magisk / Webroot. Исправлена конфигурация `httpd.conf` BusyBox и обработка пустых успешных ответов команд.

# Список изменений zapret2-android v3.2.29 (3149)

- **Оптимизация списков приложений:** Удалены `com.google.android.webview` и `com.android.chrome` из каталога автоматического туннелирования для исключения непреднамеренной перемаршрутизации общего веб-трафика и системных WebView.

# Список изменений zapret2-android v3.2.28 (3148)

- **Надёжность связи WebUI в Magisk / MMRL / Webroot:** Полностью переработан механизм связи HTTP API. Добавлено автоматическое распознавание всех локальных портов (8080, 8088), относительных путей WebView, очистка CGI-заголовков, защита таймаутом и поддержка bridge `window.mmrl` / `window.magisk`.

# Список изменений zapret2-android v3.2.27 (3147)

- **Исправление WebUI в Magisk / Browser:** Исправлена ошибка разбора ответа `Unexpected token 'l', "local modd"... is not valid JSON` при обращении к API через BusyBox httpd. Удалён лишний статический скрипт `api.lua`, приоритет и валидация ответов переведены на стандартный CGI `api.sh`.

# Список изменений zapret2-android v3.2.26 (3146)

- **Точечный обход:** Встроен туннель для любых выбранных приложений (мессенджеры, Discord, AI-клиенты ChatGPT/Claude, браузеры, игры) и голосовых/медиа сетей.
- **Обновление WebUI:** Полная переработка интерфейса — понятные статусы, скрытие сырых технических параметров маскировки под спойлер «Дополнительные параметры», русификация терминов (Рукопожатие).
- **Быстрые фильтры приложений:** Добавлены удобные фильтры-чипы (Все, Популярные, В туннеле, В обходе, Исключения).
- **Раздача интернета:** Улучшена секция настройки раздачи по Wi-Fi/USB и защита от утечек.
- **База знаний и FAQ:** Встроен интерактивный раздел с ответами на частые вопросы и руководством по настройке.

# Список изменений zapret2-android v3.0.3 (3030)

- **Android 16 Cellular Interface Fix:** Исправлено определение активного интерфейса сотовой сети на Android 16 (ColorOS/OxygenOS), когда `dumpsys connectivity` возвращает `Active default network: none`.
- **Fast Background Cache Verification (Идея 1):** При восстановлении кэшированной стратегии модуль мгновенно применяет её без задержек, а в фоне выполняет быструю проверку связи (1 запрос к YouTube). Если оператор заблокировал стратегию, автоматически запускается полный переподбор и обновляется кэш.
- **Safe Fallback to COMPAT_FAKE (Идея 2):** Если во время переподбора стратегии не дали преимуществ, модуль автоматически откатывается на проверенный универсальный профиль `COMPAT_FAKE` (`strategy_2`), исключая падение сети.
- **Network Debounce / Hysteresis (Идея 3):** Добавлена задержка 3 секунды для объединения частых всплесков netlink-событий (переключение вышек сотовой связи / скачки SIM 1 <-> SIM 2).
- **Auto Cache Pruning (Идея 4):** Автоматическая очистка кэш-файлов сетей, не посещавшихся более 30 дней.
- **Re-entrant acquire_lock Fix:** Исправлена блокировка рекурсивного reload в `service.sh`, предотвращавшая запуск `nfqws2` при смене режимов DIRECT <-> DESYNC.

# Список изменений Zapret2 eCubz v3.0.2 (3020)

- Устранено незащищенное переходное окно при смене сети: при переключении с Wi-Fi (DIRECT) на мобильную сеть модуль немедленно активирует дефолтный защитный профиль (PROBING) на время тестирования стратегий.
- Исправлена отображаемая информация в WebUI во время пробирования новой сети.
- Исправлена совместимость скрипта установки (update-binary) с KernelSU, KernelSU Next и APatch.
- Добавлен надежный поиск `unzip` и `busybox` в установщике.
- Добавлено восстановление после зависшего reload и подробные этапы очистки firewall в startup state/boot trace.
- Исправлены повреждённые русские сообщения состояния и ошибки WebUI/CLI.
- Восстановлены оригинальные ELF-бинарники `nfqws2` для arm, arm64, x86 и x86_64, повреждённые ошибочной нормализацией окончаний строк.
- Внедрен `probe_hosts.list` — простой вынос доменов для активного тестирования сети (probe).
- Внедрен `wifi_direct_ssids.list` — автоматическое выключение обхода (режим DIRECT) на доверенных Wi-Fi сетях по SSID.
- Обновлены списки приложений: внесены реальные пакеты заблокированных в РФ сервисов из Google Play (Mega.nz, Instagram, Threads, X/Twitter, Facebook, Discord, Reddit, Spotify, Twitch, Signal).
- Убраны гео-заблокированные на стороне сервиса ИИ-приложения (ChatGPT, Claude), требующие прокси/VPN.
- Режим DIRECT динамически отключает обход на нефильтрованной сети и на доверенном Wi-Fi.
